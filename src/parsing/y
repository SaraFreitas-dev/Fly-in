=========================================================
3) parse_zone()
=========================================================

INPUT example:

    hub: roof1 3 4 [zone=restricted color=red]

---------------------------------------------------------
STEP 1 → Separate metadata
---------------------------------------------------------

IF "[" exists:

    split into:

        main_part
        metadata_part

ELSE:

    metadata is empty

---------------------------------------------------------
STEP 2 → Parse main section
---------------------------------------------------------

Example main_part:

    hub: roof1 3 4

SPLIT by spaces

Expected result:

        self.name = name
        self.x = x
        self.y = y

VALIDATE:
    correct amount of values
    coordinates are integers
    zone name valid
    zone name unique

---------------------------------------------------------
STEP 3 → Parse metadata
---------------------------------------------------------

CALL:
    parse_metadata(metadata_part)

Expected metadata:
        self.zone_type = zone_type
        self.color = color

---------------------------------------------------------
STEP 4 → Apply default values
---------------------------------------------------------

IF zone type missing:
    use default

IF color missing:
    use default

IF capacity missing:
    use default

---------------------------------------------------------
STEP 5 → Validate metadata
---------------------------------------------------------

VALIDATE:
    zone type valid
    capacity > 0

---------------------------------------------------------
STEP 6 → Create Zone object
---------------------------------------------------------

CREATE Zone object

---------------------------------------------------------
STEP 7 → Store zone
---------------------------------------------------------

STORE inside:

    self.zones

IF zone is start_hub:
    self.start_zone = zone

IF zone is end_hub:
    self.end_zone = zone


=========================================================
4) parse_connection()
=========================================================

INPUT example:

    connection: A-B [max_link_capacity=2]

---------------------------------------------------------
STEP 1 → Separate metadata
---------------------------------------------------------

split:
    main_part
    metadata_part

---------------------------------------------------------
STEP 2 → Parse connection
---------------------------------------------------------

REMOVE:
    "connection:"

SPLIT by "-"

Expected:

    zone_a
    zone_b

---------------------------------------------------------
STEP 3 → Validate connection
---------------------------------------------------------

VALIDATE:

    both zones exist
    zones are different
    connection not duplicated

---------------------------------------------------------
STEP 4 → Parse metadata
---------------------------------------------------------

CALL:
    parse_metadata()

GET:

    max_link_capacity

---------------------------------------------------------
STEP 5 → Apply defaults
---------------------------------------------------------

IF no capacity:
    use default

---------------------------------------------------------
STEP 6 → Create Connection object
---------------------------------------------------------

CREATE Connection object

---------------------------------------------------------
STEP 7 → Store connection
---------------------------------------------------------

ADD to:

    self.connections

UPDATE graph adjacency structure


=========================================================
5) parse_metadata()
=========================================================

INPUT example:

    zone=restricted color=red

---------------------------------------------------------
STEP 1 → Split metadata
---------------------------------------------------------

SPLIT by spaces

Expected:

    [
        "zone=restricted",
        "color=red"
    ]

---------------------------------------------------------
STEP 2 → Parse key/value pairs
---------------------------------------------------------

FOR each metadata entry:

    SPLIT by "="

VALIDATE:
    valid format

STORE in dictionary


=========================================================
7) Graph Construction
=========================================================

AFTER parsing connections:

STORE adjacency structure

Example:

graph = {
    "hub": ["roof1", "corridorA"],
    "roof1": ["hub", "roof2"],
}


=========================================================
8) Final Goal
=========================================================

Transform this:

    raw txt file

INTO:

    Zone objects
    Connection objects
    graph structure
    validated simulation world